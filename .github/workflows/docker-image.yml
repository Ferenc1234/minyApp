name: Build and push Docker image

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          # NOTE: ghcr requires image repository names to be lowercase.
          # We hardcode the lowercased image path to avoid case issues.
          tags: |
            ghcr.io/ferenc1234/minyapp:latest
            ghcr.io/ferenc1234/minyapp:${{ github.sha }}
          file: ./Dockerfile

      - name: Show resulting image tags
        run: |
          echo "Pushed image tags:"
          echo "  ghcr.io/ferenc1234/minyapp:latest"
          echo "  ghcr.io/ferenc1234/minyapp:${GITHUB_SHA}"
